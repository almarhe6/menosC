// Ejemplo de uso: operadores aritmeticos.
// Debe devolver el mismo numero elevado al cuadrado
//--------------------------------------------------
int b;
int c;
bool x;
int casdf;


int main ()
{ int a;
int z;
b = 7;

  read(a);
  a = (((((2 * a) - a) + a) / 2) * a);
  print(a);

  return 0;
}
